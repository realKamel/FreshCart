import { HideOnClickOutsideDirective } from './hide-on-click-outside.directive';

describe('HideOnClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new HideOnClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
